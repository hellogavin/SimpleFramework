module("network", package.seeall)

local islogging = false;

---Web消息--
function OnWeb(key, value)
	--ModuleName.function--
end

--Socket消息--
function OnSocket(key, buffer)
	--ModuleName.function--
end

